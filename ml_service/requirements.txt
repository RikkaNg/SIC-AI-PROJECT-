# Phiên bản KHỚP với môi trường đã train các pickle trong ml_service/models
# (sklearn 1.9 / numpy 2.5 / lightgbm 4.7). Lệch version giữa runtime và môi
# trường train làm joblib.load được nhưng transform/predict lỗi
# (VD: 'ColumnTransformer' object has no attribute '_name_to_fitted_passthrough').
# Yêu cầu Python >= 3.11 (xem ml_service/Dockerfile).
fastapi==0.141.1
uvicorn==0.52.4
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
lightgbm==4.7.0
catboost==1.2.10
joblib==1.5.3
pydantic==2.13.5
httpx==0.28.1
